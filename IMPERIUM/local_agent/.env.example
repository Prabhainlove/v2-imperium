# Local Imperium agent — fully offline. No Supabase, no API keys required.
HOST=127.0.0.1
PORT=8000
HEADLESS=0
# Optional: where to persist run state across restarts
STATE_FILE=./agent_state.json
# Optional: allow the web UI origin (comma separated). * = allow all (dev only).
CORS_ORIGINS=*

# Chrome profile reuse — keeps you logged in to LinkedIn, Google, etc.
# Set USE_DEFAULT_CHROME=1 to reuse your real Chrome's User Data folder.
# IMPORTANT: fully close your normal Chrome before starting the agent,
# otherwise Chrome refuses to open the same profile twice.
USE_DEFAULT_CHROME=1
# Leave blank to auto-detect for your OS:
#   Windows: C:\Users\<you>\AppData\Local\Google\Chrome\User Data
#   macOS:   ~/Library/Application Support/Google/Chrome
#   Linux:   ~/.config/google-chrome
CHROME_USER_DATA_DIR=
CHROME_PROFILE_DIR=Default
